#include <iostream>
using namespace std;

int main(){
    int a,b;
    a=2, b=4;

    //arithmatic
    /*cout << a+b << endl;
    cout << a-b << endl;
    cout << a*b << endl;
    cout << a/b << endl;
    cout << a%b << endl;

    //during modulo operation, the sign of the left operand will be the sign of operation ans
    cout << a%b << endl;
    cout << (-a%b) << endl;
    cout << (a%-b) << endl;
    cout << (-a%-b) << endl;

    //logical
    cout << (a<b) << endl;
    cout << (a>b) << endl;
    cout << (a<=b) << endl;
    cout << (a>=b) << endl;
    cout << (a==b) << endl;
    cout << (a!=b) << endl;

    //relational
    cout << (0 && 0) << endl;
    cout << (1 && 0) << endl;
    cout << (1 && 1) << endl;

    cout << (0 || 0) << endl;
    cout << (0 || 1) << endl;
    cout << (1 || 1) << endl;
    cout << (!1) << endl;
    cout << (!0) << endl;
    */
    //bitwise (& | << >> ~ ^)

    //ternary condition ? expression1 : experssion2

    //uinary ++ -- post pre

    //shortcut assignment operators  +=  -=  *=  = 

    //other random operators
    // sizeof 
    // & (address of operator)
    // * (dereferencing operator)
    // new, delete (dynamic memory location allocators)  
    // -> (arrow operator)
    // . (dot operator)


    //put more and more examples
    //get the precedance of operatoras pdf link here:-

    return 0;
}